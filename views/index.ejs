<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Expense Tracker</title>
  <link rel="stylesheet" href="/styles.css">
  <title>Expense Tracker | Manage Your Daily Expenses</title>
<meta name="description" content="Expense Tracker App to manage your daily expenses easily.">
<meta name="keywords" content="Expense Tracker, Budget, Daily Expenses, Finance App, harinad, Expense Tracker harinand singh">
<meta name="author" content="Harinand Kumar">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="google-site-verification" content="J5tWD6yYhusB6-J2b_cZuB3Qa66YIWjPZOC8zZCY_Mc" />
  <style>


    /* You can move this into styles.css */

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f4f7fa;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 900px;
      margin: 40px auto;
      background: #fff;
      padding: 25px 30px;
      border-radius: 16px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.1);
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
      font-size: 2rem;
      color: #333;
    }

    .summary {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 25px;
      flex-wrap: wrap;
    }

    .monthly-total {
      font-size: 1.2rem;
      font-weight: bold;
      color: #2d6a4f;
      margin-bottom: 10px;
    }

    .add-form {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }

    .add-form input, .add-form button {
      padding: 10px 12px;
      border-radius: 8px;
      border: 1px solid #ddd;
      font-size: 0.95rem;
    }

    .add-form input:focus {
      border-color: #2d6a4f;
      outline: none;
    }

    .add-form button {
      background: #2d6a4f;
      color: #fff;
      border: none;
      cursor: pointer;
      transition: background 0.3s;
    }

    .add-form button:hover {
      background: #1b4332;
    }

    h2 {
      margin: 20px 0 10px;
      font-size: 1.5rem;
      color: #333;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
      border-radius: 10px;
      overflow: hidden;
    }

    table thead {
      background: #2d6a4f;
      color: #fff;
    }

    table th, table td {
      padding: 12px;
      text-align: center;
      border-bottom: 1px solid #eee;
    }

    table tr:nth-child(even) {
      background: #f9f9f9;
    }

    table tr:hover {
      background: #eef6f2;
    }

    table button {
      padding: 6px 12px;
      border: none;
      border-radius: 6px;
      background: #d9534f;
      color: #fff;
      cursor: pointer;
      transition: background 0.3s;
    }

    table button:hover {
      background: #c9302c;
    }

    .empty-msg {
      text-align: center;
      padding: 20px;
      color: #777;
      font-style: italic;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>💰 Expense Tracker</h1>

    <div class="summary">
      <div class="monthly-total">
        This month's total: <span class="amount">₹<%= monthlyTotal %></span>
      </div>

      <form class="add-form" action="/add" method="POST">
        <input type="text" name="title" placeholder="Expense title" required>
        <input type="number" name="amount" placeholder="Amount" step="0.01" required>
        <input type="text" name="category" placeholder="Category (Food/Travel/...)" required>
        <button type="submit">➕ Add</button>
      </form>
    </div>

    <h2>All Expenses</h2>
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Amount</th>
          <th>Category</th>
          <th>Date</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% if (expenses.length === 0) { %>
          <tr><td colspan="5" class="empty-msg">No expenses yet 🚫</td></tr>
        <% } %>

        <% expenses.forEach(function(exp) { %>
          <tr>
            <td><%= exp.title %></td>
            <td>₹<%= exp.amount %></td>
            <td><%= exp.category %></td>
            <td><%= new Date(exp.date).toLocaleString() %></td>
            <td>
              <form action="/delete/<%= exp._id %>" method="POST" onsubmit="return confirm('Delete this expense?')">
                <button type="submit">🗑 Delete</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
